# Simple benchmark to see a time and memory usage

## How it work

add `banch.rb` file to repo and load file:

```
require_relative 'bench.rb'
```

calls your code or method with:

```
bench { code_for_benchmark }
```
